# Current Feature

<!-- Feature Name -->

Rate Limiting for Auth

## Status

<!-- Not Started|In Progress|Completed -->

In Progress

## Goals

<!-- Goals & requirements -->

- Reusable rate limiting utility (`src/lib/rate-limit.ts`) built on Upstash Redis + `@upstash/ratelimit`, sliding window algorithm
- Rate limit these auth endpoints:
  - `/api/auth/callback/credentials` (login) — 5 attempts / 15 min, keyed by IP + email
  - `/api/auth/register` — 3 attempts / 1 hour, keyed by IP
  - `/api/auth/forgot-password` — 3 attempts / 1 hour, keyed by IP
  - `/api/auth/reset-password` — 5 attempts / 15 min, keyed by IP
  - `/api/auth/resend-verification` — 3 attempts / 15 min, keyed by IP + email
- Exceeding the limit returns 429 with `{ error: "Too many attempts. Please try again in X minutes." }` and a `Retry-After` header
- Rate limit checks return `{ success, remaining, reset }`
- Frontend surfaces the 429 via a toast
- Fail open (allow the request) if Upstash is unreachable

## Notes

<!-- Any extra notes -->

Spec: `context/features/rate-limiting-spec.md`

- New env vars: `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN` (documented in `.env.example`); both were already set in the local `.env`, so testing ran against real Upstash Redis rather than the fail-open path.
- Resolved the open question from `load`: `/api/auth/callback/credentials` is NextAuth's own route, not one of ours, so the login limit is checked inside the Credentials provider's `authorize(credentials, request)` in `src/auth.ts` — `request` is the raw `Request`, confirmed from `@auth/core`'s type declarations, giving access to the same IP-extraction helper the route handlers use. A rate-limited attempt throws a new `RateLimitedError` (a `CredentialsSignin` subclass, `code: "rate-limited"`), mirroring the existing `EmailNotVerifiedError` pattern; the check runs after zod-parsing credentials (need the email to build the IP+email key) but before the Prisma lookup/bcrypt compare, so a limited request never touches the DB.
- Per-route checks only, no middleware — matches the "out of scope for this pass" note from `load`.
- Implemented: `src/lib/rate-limit.ts` (Upstash `Redis` + `Ratelimit.slidingWindow`, one limiter per endpoint via `rateLimiters`, `checkRateLimit` fails open — returns `{success: true, remaining: Infinity, reset: 0}` — both when Upstash env vars are unset and when the Upstash call itself throws, `getClientIp` reading `x-forwarded-for`/`x-real-ip`, `rateLimitExceededResponse` building the 429 with `Retry-After` in seconds and the `{error: "Too many attempts. Please try again in X minutes."}` body); wired into `register`, `forgot-password`, `reset-password` (IP only), `resend-verification` (IP+email, checked after schema validation so the key uses the validated address) and the credentials `authorize` (IP+email, as above).
- Frontend: `SignInForm`'s `ERROR_MESSAGES` gained a `rate-limited` entry and now also fires `toast.error` for that specific code (every other sign-in error stays inline-only, unchanged); `RegisterForm` and `ResetPasswordForm` already had an inline-error branch on a failed response, extended to also toast when `response.status === 429`; `ForgotPasswordForm` and `SignInForm`'s `handleResendVerification` previously ignored the response body entirely (deliberate, to not leak account existence) — both now special-case `response.status === 429` to toast the server's message before falling into the generic "check your inbox" success state, since a 429 doesn't depend on whether the account exists and so isn't an enumeration vector.
- Verified live against real Upstash Redis rather than assumed: `resend-verification` 429s on the 4th call within its 3/15min window with a `Retry-After` header matching the body's "X minutes"; `register` 429s on the 4th call within its 3/hour IP window even across different emails, confirming the key is IP-only; the credentials login path was driven through NextAuth's real CSRF+callback flow (`/api/auth/csrf` then `POST /api/auth/callback/credentials`) and the 6th attempt's redirect `Location` carried `code=rate-limited`, confirming the 5/15min window and the `authorize`-level check. A Playwright pass on `/sign-in` confirmed the browser-side UI: the inline alert renders the exact "Too many sign-in attempts..." text, and the sonner toast fires with the same message (visible in the a11y snapshot immediately after the rate-limited submit; a separate check on `/sign-in?verified=1` confirmed the toast mechanism itself renders fine, ruling out a toast-specific regression — the rate-limit toast's own screenshot just missed sonner's ~4s auto-dismiss window). No console errors.
- Left four throwaway accounts in the `development` Neon branch from register-endpoint rate-limit testing (`ratelimit-reg-1@example.com` through `-3@example.com`, all created before the 4th call 429'd) — not cleaned up since deleting rows needs explicit go-ahead per `CLAUDE.md`, same precedent as prior auth-phase sessions.
- Not tested: behavior when Upstash is genuinely unreachable (network failure rather than unset env vars) — the fail-open `catch` in `checkRateLimit` is implemented but only exercised by inspection, not a live outage.

## History

<!-- Keep this updated. Earliest to latest -->

- Project setup and boilerplate cleanup
- Initial Next.js 16 + Tailwind CSS v4 setup (App Router, TypeScript strict); committed and pushed to GitHub
- Mock data as the single source of truth for the dashboard UI (@src/lib/mock-data.ts); committed and pushed to GitHub
- Dashboard UI Phase 1 (layout shell): ShadCN init (radix base, nova preset), `/dashboard` route and layout, top bar with search and buttons, placeholder sidebar/main, dark mode by default, Geist wired to `--font-sans`
- Dashboard UI Phase 2 (sidebar): ShadCN sidebar/avatar/collapsible/separator, collapsible Types group linking to `/items/[type]`, Collections group with favorites and recent, brand header, user footer, top bar drawer trigger with cookie-persisted state, icon rail on desktop and Sheet drawer on mobile
- Dashboard UI Phase 3 (main area): ShadCN card/badge, 4 stats cards, collections grid with per-collection type icons, pinned items, 10 most recent items, shared section header, derived data helpers in @src/lib/dashboard.ts
- Feature-driven architecture: `src/features/[feature]/` (items, collections, dashboard, user) each owning components/lib/types behind an `index.ts` barrel, app shell chrome moved to @src/components/layout, `src/lib/dashboard.ts` split across the features, domain types moved out of @src/lib/mock-data.ts, architecture notes rewritten in @CLAUDE.md and file organization rules updated in @context/coding-standards.md
- Neon PostgreSQL + Prisma ORM setup: Prisma 7 (`prisma-client` generator into @src/generated/prisma, datasource URL in `prisma.config.ts`, Neon driver adapter), initial schema for `User`/`Item`/`ItemType`/`Collection`/`Tag`/`ItemTag` plus NextAuth `Account`/`Session`/`VerificationToken`, foreign-key and lookup indexes, cascade rules (user-owned cascades, `Item.collection` set null, `Item.type` restrict), client singleton in @src/lib/prisma.ts, `.env.example` documenting `DATABASE_URL`/`DIRECT_URL`, `init` migration created and applied to the Neon development branch
- Database seed script: `prisma/seed.ts` run through `migrations.seed` in @prisma.config.ts via tsx, demo user (bcryptjs, 12 rounds), 7 system item types, 5 collections and 18 items with 21 reused tags via `connectOrCreate`, idempotent by rebuilding only the demo user's rows; `StickyNote` added to `ITEM_TYPE_ICONS` and @src/lib/mock-data.ts icons/colours synced to the spec
- Plural snake_case database naming: `@@map`/`@map` across the schema (model names stay singular so the client is still `prisma.user`), applied by a hand-generated migration of 85 `ALTER ... RENAME` statements instead of the drop/create Prisma emits for `@@map` changes, so no rows were lost; verified with `prisma migrate diff` reporting no difference
- Dashboard collections from the database: @src/features/collections/lib/collections.ts swapped from mock data to Prisma (`getFavoriteCollections`, `getRecentCollections`, new `getCollectionStats`), every query scoped to the seeded demo user by email as a stand-in until NextAuth lands; card accent colour derived from the type a collection holds most of (the `collections.color` column stays unused), with the type icon row and item count coming from `_count.items` and one `item.groupBy` for the whole grid instead of a lookup per card; `CollectionsNav` split into an async server component that fetches and a client `CollectionsNavGroup`, keeping Prisma out of the client bundle; the two collection stats cards read from the database while item stats stay on mock data; unused `collections` array dropped from @src/lib/mock-data.ts. Data access lives in the feature's `lib/` rather than the `src/lib/db/collections.ts` named in @context/features/dashboard-collections-spec.md, to keep the one-data-layer-per-feature rule in @CLAUDE.md
- Dashboard items from the database: @src/features/items/lib/items.ts swapped from mock data to Prisma (`getPinnedItems`, `getRecentItems`, `getItemTypes` now async, new `getItemStats`), every query scoped to the seeded demo user by email as a stand-in until NextAuth lands; list queries select only what a row renders, so the `content` payload stays in the database, and embed the item's type so `ItemRow` gets its icon and left border without a lookup per row; domain types reworked into `ItemSummary` (list shape, no content), `ItemTypeSummary` (the type embedded on an item) and `ItemType` (adds `isSystem` and an `itemCount` from a `_count` filtered to the user's items), plus `ItemStats`; item types are ordered by `createdAt` because the table has no sort column, which keeps the built-in types in the order the spec lists them; `ItemTypesNav` split into an async server component that fetches and a client `ItemTypesNavGroup`, mirroring `CollectionsNav`, so the sidebar counts come from the database too — beyond the spec, agreed before implementing; the item stats cards now read from the database, the dashboard page fetches collections, pinned and recent in one `Promise.all`, and the `items`/`itemTypes` arrays are gone from @src/lib/mock-data.ts, which is down to `currentUser`. Data access lives in the feature's `lib/` rather than the `src/lib/db/items.ts` named in @context/features/dashboard-items-spec.md, to keep the one-data-layer-per-feature rule in @CLAUDE.md. @src/lib/sort.ts has no callers left now that ordering happens in SQL.
- Stats & sidebar from the database: most of @context/features/stats-sidebar-spec.md was already satisfied by the two previous features — the stats cards, the sidebar item types with their icons linking to `/items/[slug]`, and the sidebar collections all read from Prisma already, checked against the seeded data rather than assumed (the sidebar type counts sum to the 18 items the stats card reports). New work was a "View all collections" entry at the foot of the Collections group, using `FolderOpen` so it stays distinct from the per-collection `Folder` once the sidebar collapses to the icon rail, and a dominant-type colour dot on recent collections: it takes the `SidebarMenuBadge` slot so it parallels the star that favourites keep, which drops the item count from the sidebar — the count stays in the hover tooltip. The dot reuses `Collection.color`, already derived from the most-used type and the same value as the card's left border, falling back to `var(--color-muted-foreground)` for a collection with no items; `CollectionList`'s `showFavoriteIcon` boolean became a `badge: "favorite" | "type-color"` prop. No `src/lib/db/items.ts` as the spec names — the item queries stay in @src/features/items/lib/items.ts per the one-data-layer-per-feature rule in @CLAUDE.md, the same call as the two previous features, and every database function the spec asks for already existed.
- Pro badge in the sidebar: `Files` and `Images` carry an uppercase `PRO` badge in the Types group, from @context/features/add-pro-badge-sidebar.md. Which types are Pro-gated is not in the data — `item_types` has no plan column — so `PRO_ITEM_TYPE_NAMES` in @src/features/items/lib/item-types.ts lists them by name behind an `isProItemType` predicate, kept out of the feature barrel since only `ItemTypesNavGroup` needs it and it is the one place to change when the plan model lands. The badge is the ShadCN `Badge` at `variant="outline"`, shrunk to `h-4 px-1 text-[0.7rem]` with `text-muted-foreground` so it sits in the same size register as the sidebar's own FAVORITES/RECENT labels; it renders inline after the type name inside the `Link`, so the `SidebarMenuBadge` slot keeps the item count and both read on the same row. The name span now carries `truncate` explicitly, because `SidebarMenuButton` applies truncation through `[&>span:last-child]:truncate` and the badge takes that slot on Pro rows. The icon rail needs no special casing — the button's `overflow-hidden` clips the badge along with the name — so the collapsed tooltip gains `· Pro` to carry the information instead. Note against the spec: @context/project-overview.md puts image uploads on the Free tier and only file uploads on Pro, so badging `Images` follows the spec rather than the pricing table.
- Codebase audit quick wins: the low-risk half of a full-codebase audit, ten fixes on one branch. Correctness — @src/features/collections/lib/collections.ts now scopes the collection `_count` and the item `groupBy` to the current user instead of trusting that a collection only ever holds its owner's items (`Item.collectionId` has no composite foreign key to `Item.userId`, so the database does not enforce it); that new predicate is why `Item` gained `@@index([collectionId, userId])` and migration `20260826121105_item_collection_user_index`, one additive `CREATE INDEX` applied to the dev branch with `migrate dev` and to the prod branch with `migrate deploy`, both reporting no drift. `server-only` guards @src/lib/prisma.ts and the two feature data layers, so a client component importing the items barrel now fails the build by name rather than quietly dragging the Neon adapter into the browser bundle — verified with a throwaway client probe, not assumed. `User.name` became `string | null` to match `name String?` in the schema and `getInitials` took an email fallback, so the first OAuth account without a name stops throwing on `.split(" ")`. Accessibility — `role="img"` next to `aria-label` on lucide's bare `<svg>`: `ItemTypeIcon` as planned, plus the star and pin in `ItemRow`, `CollectionCard` and `CollectionsNavGroup`, which carried the identical defect; 34 labelled icons on the dashboard, all 34 now exposed. Cleanup — dead `src/lib/sort.ts` deleted, `format.ts` moved to @src/features/items/lib/format.ts and `mock-data.ts` to @src/features/user/lib/mock-user.ts, which retires the import-cycle exemption that `src/lib/mock-data.ts` used to need; an explicit `select` in `findCollections` matching the one `findItems` already had; and `--font-mono` mapped in the `@theme inline` block so the Geist Mono webfont the layout already downloads is finally reachable from `font-mono`. Docs — five stale statements corrected in @CLAUDE.md, including the claim that there is no database. Against the plan: goal 8 was written as "drop the barrel exports nothing outside consumes", but trimming `ItemStats` while adding `CollectionStats` is incoherent when both are return types of barrel-exported functions, so the rule applied instead was that a barrel exports the types its functions return — `CollectionStats` and `CollectionItemType` added, nothing removed. Deferred to their own features, with reasons recorded at the time: the unbounded `getPinnedItems`/`getFavoriteCollections`, the item types fetched four times per dashboard render (wants a `cache()`-wrapped lookup), the seed's hardcoded password and missing `NODE_ENV` guard, the 448-line `prisma/seed.ts`, the dead `collections.color` column, and `prisma.config.ts` loading `.env` while `next build` prefers `.env.production` — that last one is why deploying a migration to prod currently needs an explicit `DIRECT_URL=<prod url> npx prisma migrate deploy`.
- Auth Phase 1 - NextAuth v5 + GitHub provider: `next-auth@beta` (not `@latest`, which installs v4) plus `@auth/prisma-adapter`, split into an edge-compatible @src/auth.config.ts (providers only) and the full @src/auth.ts (Prisma adapter, `session: { strategy: "jwt" }`, `jwt`/`session` callbacks copying `user.id` onto the token and session), following https://authjs.dev/getting-started/installation#edge-compatibility. GitHub is the only provider. Handlers re-exported from `src/app/api/auth/[...nextauth]/route.ts`; `Session.user.id` typed via module augmentation in @src/types/next-auth.d.ts. @src/proxy.ts builds its own edge-only `NextAuth(authConfig)` instance (can't import `src/auth.ts` — that one carries the Prisma adapter, not edge-safe), matches `/dashboard/:path*`, and redirects an unauthenticated request to `/api/auth/signin` with `callbackUrl` set back to the original URL. No custom `pages.signIn` — NextAuth's default page is used as-is. `AUTH_SECRET`, `AUTH_GITHUB_ID`, `AUTH_GITHUB_SECRET` documented in `.env.example`. Verified by curling a fresh dev server rather than assumed: unauthenticated `/dashboard` returns a 307 to `/api/auth/signin?callbackUrl=...`, and that sign-in page renders a "Sign in with GitHub" button — the full OAuth round-trip still needs a human to click through a real GitHub login in a browser, which wasn't possible in this session. `context/features/auth-phase-2-spec.md` and `auth-phase-3-spec.md` were already sitting in the working tree for later phases and were deliberately left uncommitted rather than swept in with `git add -A`.
- Auth Phase 2 - Credentials (email/password) provider: @src/auth.config.ts carries a placeholder Credentials provider (`authorize: () => null`, edge-safe) alongside GitHub; @src/auth.ts overrides the entire `providers` array (object-spread order means a later key wins) with the real GitHub plus a Credentials provider whose `authorize` looks the user up by email, bcrypt-compares the password, and returns `{id, name, email, image}` — the password hash itself is never returned, so it can't leak into the JWT. Input validated with a new `zod` dependency, per @context/coding-standards.md's "Validate all inputs with Zod"; `credentialsSchema` is a plain `{email, password}` shape shared by nothing else. `POST /api/auth/register` (`src/app/api/auth/register/route.ts`) sits outside `src/features/` like the rest of the auth code, imports `@/lib/prisma` directly (same precedent as `src/auth.ts`), validates with a `zod` object + `.refine` for the password-match check, hashes with bcryptjs at 12 rounds (matching `prisma/seed.ts`), and returns the `{success, data|error}` shape from @context/coding-standards.md's Error Handling section even though that section is written for Server Actions. `User.password` already existed in the schema — no migration. Verified live rather than assumed: registration succeeds and rejects a duplicate email (409) and mismatched passwords (400); signing in with the registered credentials sets a session cookie, reaches `/dashboard` (200), and `/api/auth/session` reports `user.id`; a wrong password redirects with `error=CredentialsSignin` and creates no session; the GitHub button is still on `/api/auth/signin` afterward. `context/features/auth-phase-3-spec.md` again left uncommitted for its own phase.
- Auth Phase 3 - Sign In, Register & Sign Out UI: custom `/sign-in` (email/password, "Sign in with GitHub", link to register, inline error display) and `/register` (name/email/password/confirm, posts to `/api/auth/register`, redirects to `/sign-in` on success) replace NextAuth's default pages — `pages.signIn: "/sign-in"` added to @src/auth.config.ts so NextAuth's own redirects land there too, not just @src/proxy.ts's protected-route one (updated from `/api/auth/signin` to `/sign-in`). Auth UI components (`AuthCard`, `SignInForm`, `RegisterForm`, a small inline-SVG `GitHubIcon` since this `lucide-react` version has no GitHub brand icon) live in `src/components/auth/`, not a `src/features/auth/` — there's no auth feature `lib/`, auth code sits outside `src/features/` per the Phase 1/2 precedent. Sidebar footer's `getCurrentUser()` in @src/features/user/lib/user.ts now reads `auth()` for the session id and queries Prisma for the full row (needed for `isPro`, which isn't in the JWT), retiring the last mock stand-in — `mock-user.ts` deleted; `UserAvatar` (GitHub image or initials) split out as its own reusable component; a new `SidebarUserMenu` wraps a shadcn `dropdown-menu` (added, along with `label`, neither existed yet) so avatar click opens a "Sign out" menu using `next-auth/react`'s client `signOut`, while the existing gear icon becomes a `Link` to `/profile` — the spec's "clicking on the icon should go to /profile" read as that icon, not the avatar, since the avatar's click is already spoken for by the dropdown; `/profile` itself stays a deliberate 404. Added shadcn's `sonner` for toasts (coding-standards.md already called for toast-based messaging but nothing was installed yet) — mid-review the ask expanded to a success toast on registration, so `RegisterForm` fires `toast.success("Account created", …)` before the `router.push("/sign-in")`, and the `<Toaster />` mounted in the root layout keeps it visible across that client-side navigation; the shadcn-generated `Toaster` wrapper depended on `next-themes` for light/dark switching, which this project doesn't use (dark mode is hardcoded on `<html>`, no toggle), so that dependency was stripped back out and the toaster hardcoded to `theme="dark"` instead of adding an unused provider. Verified live end-to-end with Playwright rather than assumed: register → sign-in → dashboard round trip, sidebar showing the real user (name/email/initials fallback), avatar-dropdown sign-out clearing the session (confirmed by `/dashboard` redirecting again), wrong-password and duplicate-email errors surfacing inline, the registration toast appearing and surviving the redirect to `/sign-in`, and the GitHub button reaching GitHub's real OAuth authorize screen with the right client id/scopes/callback — same as Auth Phase 1, the full round-trip still needs a human login. Left two test accounts (`playwright-tester@example.com`, `toast-tester@example.com`) in the `development` Neon branch from this verification, not cleaned up since deleting rows needs explicit go-ahead per @CLAUDE.md.
- Email verification on register: `resend` added as a dependency; a new `src/lib/resend.ts` client singleton and `src/lib/verification-email.ts` (sends from Resend's shared `onboarding@resend.dev` sender — no verified domain configured yet) sit outside `src/features/`, alongside the rest of the auth code, per the Phase 1/2 precedent. `POST /api/auth/register` now also creates a token in the existing NextAuth `VerificationToken` table (`identifier`/`token`/`expires`, 24h TTL, `identifier` = email since the table isn't otherwise in use) and emails a same-origin link built from the request's own origin — no new site-URL env var needed. `GET /api/auth/verify-email` consumes the token (delete-then-check, so a replayed link always reads as invalid rather than re-verifying), sets `User.emailVerified`, and redirects to `/sign-in?verified=1`; it deliberately does not mint a session — verifying just unblocks credentials sign-in, it doesn't log the user in. `src/auth.ts`'s Credentials `authorize` now throws a custom `EmailNotVerifiedError` (a `CredentialsSignin` subclass with `code: "email-not-verified"`) when `emailVerified` is null, which next-auth's `signIn()` surfaces as `result.code` distinct from the generic `result.error`; GitHub OAuth is untouched, since the check only lives inside the Credentials callback. `POST /api/auth/resend-verification` re-sends a fresh link and always responds `{success: true}` whether or not the account exists or is already verified, so it can't be used to enumerate registered emails. `SignInForm` surfaces `email-not-verified`/`invalid-token`/`expired-token` as specific messages, shows a "Resend verification email" button for all three (a gap caught in `review` — it initially only appeared for the sign-in-block case, not for someone landing straight from a dead link), and toasts on `?verified=1`. Verified live end-to-end with Playwright rather than assumed, using a throwaway `verify-tester@example.com` account (Resend's sandbox sender 422s on real sends to non-owner addresses like this, so the token was read directly from the dev database to build the verify URL — the app-side flow doesn't depend on inbox delivery): register → blocked credentials sign-in with the resend button → verify link flips `emailVerified` and redirects with a success toast → sign-in then succeeds → replaying the same link redirects as invalid. Left that one verified test account in the `development` Neon branch, not cleaned up since deleting rows needs explicit go-ahead per @CLAUDE.md.
- Fix: log the verification link in dev. Resend's sandbox sender can only deliver to the account's own address, so registering with any other email 422s silently. `sendVerificationEmail` in @src/lib/verification-email.ts now `console.log`s `email -> verifyUrl` whenever `NODE_ENV !== "production"`, before attempting the send, so the link is always available in the dev server's output regardless of whether Resend can actually deliver it. Verified live: registering with a non-owner address still 422s from Resend but the link appears in the server log, and following it verifies the account normally.
- Email verification toggle: `isEmailVerificationEnabled()` added to @src/lib/verification-email.ts (`process.env.EMAIL_VERIFICATION_ENABLED !== "false"`, so unset defaults to enabled), used in three places — `POST /api/auth/register` skips creating a `VerificationToken` and sending the email when disabled, setting `User.emailVerified` at creation instead; `src/auth.ts`'s Credentials `authorize` gates the unverified-block on the flag itself rather than just skipping it for new signups, so toggling off unblocks existing unverified accounts immediately too; `POST /api/auth/resend-verification` returns its generic success response before touching the database when disabled. `.env.example` documents `EMAIL_VERIFICATION_ENABLED`, commented out. Chosen over a DB setting/admin UI (no such surface exists) or a flags service (overkill) — matches the project's existing env-var pattern. Verified live with two separate dev server runs: disabled — register then sign in immediately succeeds, DB confirms `emailVerified` set at creation with no token row and no email attempt; enabled (default, unset) — unchanged, sign-in still blocks with the resend button.
- Forgot password: "Forgot password?" link on `SignInForm` next to the Password label, a new `/forgot-password` page (`ForgotPasswordForm`, email only) posting to `POST /api/auth/forgot-password`, and `/reset-password?token=...` (`ResetPasswordForm`, new/confirm password) posting to `POST /api/auth/reset-password`; a new @src/lib/password-reset-email.ts mirrors `verification-email.ts`'s dev-mode console.log-before-send. Reuses the existing `VerificationToken` model with no migration, as asked, but with `identifier` set to `password-reset:<email>` rather than a bare email — the deleteMany that clears old tokens before issuing a new one would otherwise be able to delete a pending email-verification token for the same user sharing that identifier, and vice versa from `resend-verification`'s own deleteMany; `reset-password/route.ts` also rejects any token whose identifier lacks that prefix, so an email-verification token can't be replayed as a password reset. Token TTL is 1 hour, shorter than email verification's 24h, since a live reset link is a higher-value target. `reset-password/route.ts` deletes the token before checking expiry, same order as `verify-email/route.ts`, so a replayed link always reads as invalid rather than re-consuming. Diverged from the plan in two spots, confirmed in `review`: both forms are client-driven `fetch()` calls rather than the email-link GET-redirect flow `verify-email` uses, so `ResetPasswordForm` toasts and pushes to `/sign-in` itself on success (mirroring `RegisterForm`) instead of round-tripping through a `?reset=1` query flag and `SignInForm`'s error-code handling. Verified live rather than assumed, against the seeded demo user: forgot-password shows the same generic message regardless of whether the account exists, the reset link appears in the dev server log, following it and setting a new password lets sign-in succeed with it, replaying the same link is rejected as invalid, and a bogus token is rejected the same way. Re-ran `prisma/seed.ts` afterward to restore the demo user's seeded password, since the reset flow had changed it during verification.
- Profile page: `/profile` (protected via @src/proxy.ts, matcher and route-check extended alongside `/dashboard`) built from @context/features/profile-spec.md. `getUserProfile()` added to @src/features/user/lib/user.ts (name, email, avatar, `createdAt`, and `hasPassword` from `password !== null`) alongside the existing `getCurrentUser()` rather than replacing it, since the sidebar only needs the smaller shape; `ProfileHeader` renders it, `ProfileStats` self-fetches item/collection stats and the type breakdown the same way `StatsCards` does on the dashboard. Two new one-way edges, `user` → `items` and `user` → `collections` (for those stats), documented in @CLAUDE.md next to the existing `dashboard` ones — nothing imports `user` in return. Change password and delete account are Server Actions in a new @src/features/user/actions.ts, kept thin (session check, zod validation) with the actual Prisma calls delegated to two new `lib/user.ts` functions, `changePassword` and `deleteAccount`, matching the "only `lib/` touches Prisma" rule; `ChangePasswordForm` is hidden per the spec when `hasPassword` is false (GitHub-only accounts), and `DeleteAccountCard` wraps a new shadcn `alert-dialog` (added via `shadcn add`) for the confirmation the spec asked for, calling `next-auth/react`'s `signOut` after a successful delete. Extracted `src/components/layout/AppShell.tsx` out of `dashboard/layout.tsx` (now a 3-line wrapper) so the new `profile/layout.tsx` could share the same sidebar/top-bar chrome without duplicating it — beyond the spec's literal ask but needed to render `/profile` inside the app shell at all. The generated `alert-dialog.tsx` imported `cn` from a standalone `cn` package instead of `@/lib/utils` like every other primitive in `components/ui/` — fixed the import and `npm uninstall cn`'d the stray dependency before committing. Known gap carried forward rather than fixed here: `ProfileStats` reuses the existing `getItemStats`/`getItemTypes`/`getCollectionStats`, which are still scoped by the hardcoded `DEMO_USER_EMAIL` (per the `CLAUDE.md` note on the dashboard's own data layer), so the usage stats are only accurate when signed in as the seeded demo user — the profile's own info (name/email/avatar/join date) does use the real session. Verified live with Playwright rather than assumed: signed in as the seeded demo user and confirmed the info, the 18/5 item/collection totals and the per-type breakdown summing to 18; changed the demo user's password, saw the wrong-current-password error message, then changed it back to restore seeded state; registered a throwaway `delete-test@example.com` account (verified via the dev-log link, `EMAIL_VERIFICATION_ENABLED` is on), deleted it through the confirmation dialog, confirmed it signed out and that a subsequent sign-in attempt with the same credentials now fails — full cascade delete working. Not tested live: the GitHub-only branch of the `hasPassword` gate, since GitHub OAuth needs a human login, the same limitation earlier auth phases hit.
- Sidebar user menu: moved the profile link into the avatar's dropdown, from an inline description rather than a spec file. The standalone gear `Button` next to the avatar in `SidebarUserMenu` (`Settings` icon, `asChild` `Link` to `/profile`, added back in Auth Phase 3) is gone; a `DropdownMenuItem asChild` wrapping that same `Link`, with a `User` icon instead of `Settings` since it now reads "Profile" rather than a settings gear, sits above a `DropdownMenuSeparator` and the existing "Sign out" item. The separator wasn't asked for explicitly but is the standard way to set a destructive action apart from a normal one, and `dropdown-menu.tsx` already exported it. Verified live: expanded sidebar shows just the avatar button with no second control next to it, the collapsed icon rail has no leftover gap where the gear used to be, opening the dropdown shows Profile → separator → Sign out in that order, and clicking Profile navigates to `/profile`. Also folded in this session's own workflow-policy change: `context/ai-interaction.md` and `.claude/skills/feature/actions/complete.md` now require asking before any commit, push, merge or branch delete except when explicitly running `/feature complete`, which still runs that sequence automatically once review passes — committed separately from the sidebar change since it's a different concern.
